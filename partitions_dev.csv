# ESP-IDF partition table for DEVELOPMENT builds.
# Standard single factory app, no OTA: the app is flashed straight to the
# 'factory' partition and boots directly (no otadata, no ota_0, no
# esp32_factory_app bootloader). Used with the "Dev" build profile, see
# scripts/build.py. The production table is partitions.csv (OTA via
# esp32_factory_app).
# Name,   Type, SubType, Offset,   Size,    Flags
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  0x7f0000,